export type LocalSearchParams = {
  counterId: string;
};

export type FAQ = {
  id: number;
  question: string;
  answer: string;
};

export type Counter = {
  id: number;
  pointer: number;
  name: string;
  qr: string;
};
